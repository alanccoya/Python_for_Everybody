# en este codigo usaremos for
fruta = "maracuya"

for letra in reversed(fruta): # "reversed(fruta)" la funcion toma una secuencia y devuelve un iterador que recorre los elementos en orden inverso.
    print(letra)
